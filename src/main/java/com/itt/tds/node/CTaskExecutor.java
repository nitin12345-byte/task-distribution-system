package com.itt.tds.node;

import com.itt.tds.core.model.TaskResult;
import java.io.File;

public class CTaskExecutor implements TaskExecutor {

    @Override
    public TaskResult execute(File file) {
        TaskResult taskResult = new TaskResult();

        return taskResult;
    }
}
